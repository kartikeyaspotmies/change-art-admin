/**
 * Public surface for the `qc-panel` module — F-FE-012.
 *
 * Scope: QC Review Queue, Decision UI (approve / reject with reason grid +
 * notes + reference files), QC History, QC Dashboard.
 *
 * Exports added per `specs/FEATURE-SPEC-F-FE-012-qc-panel.md`.
 */

export {};
